# CHESS+ MySQL Configuration Template
# Copy this file to .env and update with your specific values

# Database Type (uncomment to use MySQL instead of SQLite)
DB_TYPE=mysql

# MySQL Connection Parameters
DB_IP=localhost
DB_USERNAME=root
DB_PASSWORD=your_password_here
MYSQL_PORT=3306
DB_NAME=chess_plus

# Path settings
DB_ROOT_PATH=/path/to/databases

# ChromaDB Settings (if using custom ChromaDB server)
# CHROMA_SERVER_HOST=localhost
# CHROMA_SERVER_PORT=8000

# Existing CHESS+ Settings
# Add any existing application settings below